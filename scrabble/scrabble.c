#include <stdio.h>
#include <cs50.h>

int main(void)
{
    int d[5];
    int array_len=sizeof(d)/sizeof(d[0]);
    printf("%i \n",array_len);
    for (int i=0;i<array_len;i++)
    {
        d[i]=
    }
}
